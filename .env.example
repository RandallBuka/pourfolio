# Optional: self-hosted sync relay (npm run sync:server)
VITE_SYNC_URL=http://localhost:3847

# Optional: hosted cloud sync via Supabase (run supabase/schema.sql first)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
